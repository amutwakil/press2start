<section>
  <% games.each do |game| %>
    <div class="flex flex-row">
      <%= image_tag "cover_art/chrono trigger.jpg", alt: "regular box art" %>
      <ul>
        <h3><%= game.title %></h3>
        <li><%= game.summary %></li>
        <div>
          <ul class="flex flex-row">
            <li>Year: <%= game.year %></li>
            <li>Genre: <%= game.genre.name %></li>
          </ul>
        </div>
        <li>tags 🔖🔖🔖</li>
      </ul>
    </div>
  <% end %>
</section>